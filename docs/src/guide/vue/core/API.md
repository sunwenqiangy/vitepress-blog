# API

createApp

```ts
import './assets/main.css'

import { createApp } from 'vue'
// 根组件
import App from './App.vue'

// 创建一个应用实例
const app = createApp(App)

// 将应用实例挂载在一个容器元素中
app.mount('#app')
```

